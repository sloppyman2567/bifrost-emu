# Build artifacts
bifrost-emu
bifrost-emu-dbg
*.o
*.a
*.so
*.dSYM/

# Test binaries (regenerated from .s)
test/*.elf

# Ad-hoc diagnostic probes
ctest_real/mangle_probe.c

# C test binaries (regenerated from .c via musl toolchain)
ctest/*.elf
ctest_real/*.elf
ctest_real/**/*.elf

# Benchmarks / scripts (developer-only, not part of release tarball)
bench.sh

# External / downloaded test binaries (not part of the repo)
# NOTE: ctest_real/toybox is intentionally committed (run_tests.sh uses it
#       and symlinks it into ctest_real/realworld/), so only toybox-aarch64
#       (the downloadable build) is ignored here.
toybox-aarch64
ctest_real/realworld/
ctest_real/teeworlds/

# Downloaded musl cross-toolchain (fetched on demand by tools/fetch-musl-toolchain.sh)
tools/aarch64-linux-musl-cross/
tools/aarch64-linux-musl-cross.tgz

# Downloaded glibc cross-toolchain (fetched on demand by tools/fetch-glibc-toolchain.sh)
tools/aarch64-linux-gnu-cross/
tools/aarch64-linux-gnu-cross.tgz
tools/aarch64-linux-gnu-cross.tgz.xz

# Downloaded SDL2 .deb packages (for offline SDL2 build)
tools/sdl2-debs/

# SDL2 SDK (extracted by tools/fetch-sdl2-headers.sh)
tools/sdl2-sdk/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Agent workspace data (local tool config, node_modules, plans)
.kilo/
.opencode/

# CMake (if added later)
build/
cmake-build-*/

# Logs
*.log
trace-*.txt

# Benchmarking / debug artifacts
bifrost-fb.ppm
*.ppm
/core
bifrost-emu-o0
bifrost-emu-o1
bifrost-emu-o2
bifrost-emu-test*

# Audio dump files
*.wav

# Agent context file — local working notes for AI assistants.
# NEVER commit this file; it is for cross-session continuity only.
context.md

# Backup directory (created before file modifications for safe rollback)
.backup/
rootfs/
test_capi
busybox-glibc
busybox-musl
# Runtime artifacts
.freebuff/
.build.lock

# Ad-hoc JIT audit / media / scratch files
findings.md
subaru_stairs.mp4
subaru_stairs_mmv.mp4
ctest_real/minecraft_weekend/t
